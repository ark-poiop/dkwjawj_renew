# 자동 시장 브리핑 시스템 의존성
# Python 3.9+ 필요

# HTTP 요청
requests==2.31.0

# 날짜/시간 처리
python-dateutil>=2.8.0

# 로깅 및 유틸리티
colorlog>=6.7.0

# 데이터 처리 (선택사항)
pandas>=1.5.0
numpy>=1.24.0

# 환경변수 관리 (선택사항)
python-dotenv==1.0.0

# YAML 설정 파일 처리
PyYAML==6.0.1

# 테스트 (개발용)
pytest>=7.0.0
pytest-cov>=4.0.0 

# 네이버 증권 크롤링을 위한 라이브러리들
beautifulsoup4==4.12.2
lxml==4.9.3
selenium==4.15.2
webdriver-manager==4.0.1 